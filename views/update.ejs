<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update chat</title>
    <link rel="stylesheet" href="/update.css">
</head>

<body>
    <h1>Update Message</h1>
    <div id="main">
        <div class="card">
            <div class="mid">
                <div class="circle"></div>
                <h3>From:- <%=chat.from%>
                </h3>
            </div>
            <div class="mid">
                <div class="circle"></div>
                <h3>Recieved by <%=chat.to%>
                </h3>
            </div>
            <hr>
            <p class="date"><%=chat.created_at.toString().split(" ")[4]%>&nbsp;&nbsp; <%=chat.created_at.toString().split(" ").splice(0,3).join("-")%></p>
            <form method="POST" action="/chats/<%=chat.id%>?_method=PATCH">
                <div class="para">
                    <textarea rows="6" cols="20" class="msg" name="msg"><%=chat.msg%></textarea>
                </div>
                <button>Edit</button>
            </form>
        </div>
    </div>

</body>

</html>